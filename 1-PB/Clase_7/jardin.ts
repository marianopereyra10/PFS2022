import * as rls from "readline-sync";

let colorAula : string [] = ["Azul", "Verde", "Amarillo"];
let capacidadAula : number [] = [40, 35, 30];
let numInfantes : number = rls.questionInt("Ingrese cuantos alumnos se asignan al aula: ");
let i : number = numInfantes

for (i=1, i<capacidadAula.length[], i++){
    if(capacidadAula >= numInfantes &&)
}


